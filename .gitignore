**/.env

dataset/
!dataset/overrides/*.csv

# gridstats precomputed bundle (built once via `python -m app.gridstats.build`)
src/backend/app/gridstats/target/

# Chat usage tracing log (runtime, per-deployment)
chat_traces.jsonl
src/backend/chat_traces.jsonl